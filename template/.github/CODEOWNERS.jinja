# GitHub matches a bare email address here only if it's a verified email on
# a GitHub account with access to this repo -- if {{ owner }} hasn't
# verified this address (or it isn't the one on their GitHub account),
# reviews won't auto-request. Swap to @username if that's ever unclear.
* {{ owner }}
